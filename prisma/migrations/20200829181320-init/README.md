# Migration `20200829181320-init`

This migration has been generated by Youngsung at 8/29/2020, 6:13:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `mydb`.`Buy` ADD COLUMN `dealDate` datetime(3) NOT NULL  ;

ALTER TABLE `mydb`.`Sale` ADD COLUMN `dealDate` datetime(3) NOT NULL  ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200821215032-init..20200829181320-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Customer {
   uid String
@@ -37,8 +37,9 @@
 model Sale {
   uid String
   createdAt  DateTime @default(now())
+  dealDate DateTime
   id         Int       @default(autoincrement()) @id
   price      Float
   quantity   Float
   customer   Customer  @relation(fields: [customerId], references: [id])
@@ -62,8 +63,9 @@
 model Buy {
   uid String
   createdAt  DateTime @default(now())
+  dealDate DateTime
   id         Int       @default(autoincrement()) @id
   price      Float
   quantity   Float
   customer   Customer  @relation(fields: [customerId], references: [id])
```


